## iteracion 1
### prompt
Actualmente necesito planificar la Fase 1 del proyecto.

Revisa primero toda la documentacion del proyecto y usa como fuente de verdad principal el estado real del repositorio, especialmente:

- `others/proyecto_final.md`
- `context/project/decisions.md`
- `context/project/requirements.md`
- `context/project/phases.md`
- `context/.IA/rules.md`
- `context/.IA/architecture.md`
- `context/.IA/directory-structure.md`
- `context/.IA/constraints.md`
- `context/.IA/stack.md`

Ten en cuenta especialmente DEC-07 y DEC-10.

La Fase 1 corresponde a la implementacion Python Baseline, o sea:

- `code/python/sequential.py`
- `code/python/multicore.py`

Pero en esta conversacion no implementes todavia. Primero quiero un plan tecnico claro para ejecutar la fase correctamente, tomando como referencia el proyecto actual y no solo el enunciado original.

(Se solicita un plan que cubra como minimo: archivos a modificar/crear, funciones reutilizables compartidas, carga y validacion del dataset, medicion de tiempo excluyendo carga, registro automatico en benchmark.csv, calculo de candidates_per_second/speedup/efficiency, reproducibilidad seed=42, division de candidatos con multiprocessing, validacion de shapes/pesos/AUC/consistencia, y comandos de verificacion. CLI: --n-items, --k-candidates, --workers, --seed. Sin implementar C/MPI/OpenMP/CUDA, sin nuevas dependencias, sin refactors grandes, sin modificar la estructura del dataset.)

### respuesta
Se entrego un plan tecnico de Fase 1 (sin implementar codigo), basado en el estado real del repo. Hallazgos clave detectados como bloqueantes/dudas antes de codificar:

1. **`sequential.py` y `multicore.py` actuales son placeholders rotos**: no cargan `profiles.npy`, usan rutas planas antiguas `../data/matrix_A.npy` (no `data/n_{n_items}/`), implementan `P = A @ W[...]`/`A.mean()` en vez del modelo DEC-07 (`P=W1*T+W2*S+W3*F`, `Score=A@P`), y no tienen CLI, ni timing, ni registro en benchmark.
2. **`generate_data.py` NO inyecta senal diferencial** en las filas enfermas de A (lo exigen RF-01, constraints, DEC-08 y RIESGO-04). Con A Dirichlet puro y perfiles aleatorios, `P` es comun a todas las muestras y AUC ≈ 0.5, por lo que la validacion de Fase 1 (AUC util, consistencia ≥ 0.8) fallaria. Se marca como decision a confirmar (regenerar dataset con senal vs. dejar fuera de alcance).
3. **Conflicto de esquema en `benchmark.csv`**: el archivo actual tiene cabecera `implementacion,T_s,speedup,eficiencia,AUC` con filas plantilla, pero la Fase 1 pide `implementation,n_items,k_candidates,workers,best_auc,time_seconds,candidates_per_second,speedup,efficiency`. Se propone adoptar el esquema nuevo (append, sin sobrescribir).
4. **Firma de `random_search`**: cambiarla a `random_search(A, profiles, y, K, seed)` (y `random_search_multicore(..., workers)`) acorde a DEC-07/architecture; se documenta el cambio.

Plan propuesto: crear `code/python/common.py` (carga/validacion reutilizando los loaders de `generate_data.py`, muestreo Dirichlet, scoring DEC-07, AUC, consistencia, metricas y append a benchmark.csv); reescribir `sequential.py` y `multicore.py` para consumir ese modulo; medir tiempo con `perf_counter` solo alrededor de la busqueda; generar candidatos en el proceso principal y repartir con `np.array_split` + `multiprocessing.Pool` (determinismo independiente del numero de workers con seed=42); calcular `candidates_per_second=K/T`, `speedup=T_seq/T_impl` (leyendo la fila secuencial de benchmark.csv), `efficiency=speedup/workers`; validar shapes/pesos/AUC∈[0.5,1]/equivalencia multicore-secuencial <1e-4. Se incluyeron comandos de verificacion y se dejaron 4 dudas para confirmar antes de implementar.

## iteracion 2
### prompt
Respuestas:
1- si
2- si
3- Si
4- Si

Tarea 1
No ejecutes nada aqui, plasma toda esta planificación, en lo correspondiente en context/ (En todos los archivos que requieran) tasks, current phase, ...


### respuesta
El usuario confirmo los 4 puntos de la iteracion 1 (DEC-11 / inyeccion de senal diferencial, migracion de `benchmark.csv` a 9 columnas, creacion de `common.py`, firmas canonicas `random_search`/`random_search_multicore`).

**Tarea 1 (documentacion en `context/`, sin ejecutar codigo)**:
- `context/project/decisions.md`: agregada **DEC-11** (señal diferencial en `generate_data.py` + regeneracion de `data/n_50/` y `data/n_100/`, sin romper shapes/dtypes de DEC-10).
- `context/project/phases.md`: Fase 1 pasó de `[PENDIENTE]` a `[PLANIFICADA — lista para implementación]`, con checklist detallado referenciando `active-tasks.md`.
- `context/state/current-phase.md`: reescrito — fase activa = Fase 1, plan confirmado, codigo aun sin tocar.
- `context/state/active-tasks.md`: reescrito como plan de ejecucion paso a paso (7 tareas: DEC-11, `common.py` con firmas de todas las funciones, reescritura de `sequential.py`/`multicore.py`, migracion de `benchmark.csv`, tests pytest, comandos de verificacion y criterios de salida de Fase 1).
- `context/state/progress.md`: agregada seccion "En progreso" referenciando el plan confirmado.
- `context/state/known-issues.md`: agregados ISSUE-004 (placeholders rotos de `sequential.py`/`multicore.py`), ISSUE-005 (falta señal diferencial, resuelto via DEC-11), ISSUE-006 (esquema antiguo de `benchmark.csv`), ISSUE-007 (`code/pyproject.toml`/`uv.lock` no existen pese a estar documentados — detectado durante la exploracion del repo real).
- `context/.IA/rules.md`: seccion Benchmark actualizada con el esquema de 9 columnas; nueva seccion "Firmas canónicas Fase 1 (Python)" con las firmas de `random_search`/`random_search_multicore`, definicion de `profiles` y CLI obligatoria.
- `context/.IA/directory-structure.md`: agregados `code/python/common.py` y `code/python/tests/test_baseline.py` a la estructura; nota aclarando que `pyproject.toml`/`uv.lock` no existen aun (ISSUE-007).

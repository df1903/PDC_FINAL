# Trazabilidad — 2026-06-15 19:22

## iteracion 1
### prompt
Actualmente tenemos completas las Fases 1 (Python Baseline) y 2 (C + OpenMP). Ahora quiero implementar la Fase 3.

Revisa primero toda la documentacion del proyecto y usa como fuente de verdad principal el estado real del repositorio, especialmente: `others/proyecto_final.md`; `context/project/decisions.md` (DEC-06, DEC-10, DEC-12, DEC-13); `requirements.md`; `phases.md`; `risks.md` (RIESGO-02, RIESGO-03, RIESGO-05); `rules.md`; `architecture.md`; `coding-standards.md`; `directory-structure.md`; `constraints.md`; `stack.md`; `current-phase.md`; `scoring_openmp.c` y `npy_io.{h,c}` (Fase 2) como referencia; `scoring_mpi.c` (placeholder a reemplazar); `Makefile` (target `mpi` debe linkear `npy_io.c`); `benchmark.csv` (10 columnas con `speedup_vs_python`).

La Fase 3 corresponde a la implementacion C + MPI (`scoring_mpi.c` y su `Makefile`). En esta conversacion NO implementar todavia: primero un plan tecnico claro que cubra los 13 puntos solicitados (archivos, carga .npy + Bcast, candidatos Dirichlet con RNG SplitMix64 seed+k, distribucion de trabajo, Score/AUC reutilizados, reduccion distribuida MPI_Reduce/MPI_MAXLOC, timing MPI_Wtime + Barrier, self-test, CLI sin --threads, registro "C MPI" en benchmark, validacion equivalencia AUC, speedup P∈{1,2,4,8} ≥3× con P=4, entorno/comandos WSL2+OpenMPI). Restricciones: sin OpenMP/CUDA; sin nuevas dependencias mas alla de MPI; sin tocar npy_io ni los .npy ni filas previas/esquema del CSV; `scoring_mpi.c` < 400 LOC; sin modificar `scoring_openmp.c` ni `npy_io.{h,c}`.

### respuesta
Se entregó el **plan técnico de la Fase 3 (C + MPI)** sin implementar código, cubriendo los 13 puntos:

1. **Archivos**: reemplazo completo de `scoring_mpi.c` (placeholder de 28 LOC → < 400 LOC) y ajuste del target `mpi`/`scoring_mpi` del `Makefile` para linkear `npy_io.c`. Ningún archivo nuevo.
2. **Carga .npy**: solo rank 0 llama `load_dataset` (reutiliza `npy_io` sin cambios, DEC-06/10); difunde `n_items` + `A` (10·N f32) + `profiles` (N·3 f32) + `labels` (10 i32) con `MPI_Bcast`. No-root reserva buffers tras recibir `n_items`.
3. **Candidatos**: cada proceso genera `W_k` localmente con `sample_dirichlet(seed + k_global)` (SplitMix64, DEC-12); sin scatter de candidatos. Determinismo: `W_k` depende solo de `k` global ⇒ el conjunto evaluado es idéntico al de Fase 2, solo cambia la partición → `best_auc` idéntico para cualquier P.
4. **Distribución**: reparto contiguo `local_K = K/P`, rango `[r·local_K, (r+1)·local_K)`, último rank absorbe remanente `K - (P-1)·local_K`. Sin comunicación durante la búsqueda.
5. **Score/AUC**: copia literal de `compute_P`, `compute_score`, `compute_auc` (empates a 0.5), `scoring_consistency` desde `scoring_openmp.c`.
6. **Reducción**: recomendado `MPI_Reduce` con `MPI_MAXLOC` y `MPI_DOUBLE_INT` (transporta `best_auc` + `k*` global) → root reconstruye `best_W` con `sample_dirichlet(seed+k*)` (1 evaluación). Más simple y correcto que `MPI_MAX` + búsqueda del candidato óptimo.
7. **Timing**: `MPI_Barrier` antes de `t0 = MPI_Wtime()` (sincroniza el arranque para que el tiempo mida el proceso más lento, no el desfase de arranque); `t1` tras `MPI_Reduce`. Solo rank 0 registra.
8. **Self-test**: mismos 3 casos de `compute_auc` (incl. empate=0.875); rank 0 lo corre y `MPI_Finalize` (vía `mpirun -n 1` o ejecución directa).
9. **CLI**: `--n-items/--k-candidates/--seed/--self-test`; sin `--threads` (P lo fija `mpirun -n P`).
10. **Benchmark**: etiqueta `"C MPI"`, `workers=size`, `speedup = T_mpi(1)/T_mpi(P)` (baseline propio vía `read_last_time(..,"C MPI",..,required_workers=1,..)`), `efficiency=speedup/workers`, `speedup_vs_python = T_python_seq/T_mpi(P)`. Reutiliza `read_last_time`/`append_benchmark` (esquema 10 cols, append-only) con `MPI_Wtime`. Solo rank 0 escribe.
11. **Equivalencia AUC**: `|best_auc_MPI − best_auc_Python| < 1e-4`; trivial con dataset separable (AUC=1.0), igual que Fase 2.
12. **Speedup P∈{1,2,4,8}**: objetivo ≥3× con P=4 (RNF-03). RIESGO-05 puede materializarse: con N=50/K=100k el cómputo (~0.03 s en P=1) es tan pequeño que `MPI_Init`/`Bcast`/`Reduce` pueden dominar; mitigación: medir también K∈{500k,1M} y reportar curva speedup vs K.
13. **Entorno**: WSL2 + OpenMPI (RIESGO-02); `mpicc -O2 -Wall -Wextra -o scoring_mpi scoring_mpi.c npy_io.c -lm`; ejecutar desde `code/` con `mpirun -n P ./C_OpenMP_MPI/scoring_mpi --n-items 50 --k-candidates 100000 --seed 42`; alias `make mpi`.

Se respetan todas las restricciones (sin OpenMP/CUDA, sin tocar `npy_io`/`.npy`/filas previas/esquema CSV, < 400 LOC, sin modificar `scoring_openmp.c`). No se escribió código en esta iteración.

## iteracion 2
### prompt
Tarea 1: No ejecutes nada aquí, plasma toda esta planificación en lo correspondiente en context/ (en todos los archivos que requieran): tasks, current phase, ...

### respuesta
Se plasmó la planificación de Fase 3 en `context/` sin implementar código, y se escribió el prompt de ejecución en `instruction.md`.

**Tarea 1 — documentación actualizada:**
- `context/state/active-tasks.md`: reescrito de Fase 2 → **Fase 3 (C + MPI)**, estado `PLANIFICADA — 2026-06-15`. Incluye hallazgos verificados, 13 secciones (Makefile, reescritura de `scoring_mpi.c`, RNG por regeneración local, carga root + `MPI_Bcast`, distribución contigua, reducción `MPI_MAXLOC`, timing `MPI_Wtime`+`MPI_Barrier`, self-test, CLI sin `--threads`, registro `C MPI`, criterios de salida, RIESGO-05, entorno/comandos) y restricciones.
- `context/project/decisions.md`: añadida **DEC-14** `[CONFIRMADO — 2026-06-15]` formalizando la estrategia de Fase 3 (reutilización literal del cómputo de Fase 2 + `npy_io` sin cambios; regeneración local de candidatos en vez de `MPI_Scatter`; carga solo en root + `Bcast`; reducción `MPI_MAXLOC`/`MPI_DOUBLE_INT`; timing con `MPI_Barrier`; CLI sin `--threads`; etiqueta `C MPI`), con razón e interpretaciones descartadas.
- `context/project/phases.md`: sección Fase 3 pasó de `PENDIENTE` (con TODOs de `MPI_Scatter`/`MPI_Reduce(MPI_MAX)`) a `PLANIFICADA — 2026-06-15` con las 11 tareas refinadas alineadas a DEC-14.
- `context/state/current-phase.md`: sección "Próxima acción" actualizada — plan definido (DEC-14 + active-tasks), pendiente de implementación, con resumen de estrategia.
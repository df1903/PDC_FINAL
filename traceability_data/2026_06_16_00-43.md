## iteracion 1
### prompt
Actualmente tenemos completas las Fases 1 (Python Baseline), 2 (C + OpenMP) y 3 (C + MPI). Ahora quiero implementar la Fase 4.

Revisa primero toda la documentacion del proyecto y usa como fuente de verdad principal el estado real del repositorio, especialmente: `others/proyecto_final.md`; `context/project/decisions.md` (DEC-03, DEC-05, DEC-09, DEC-10, DEC-13); `requirements.md` (RF-03, RF-04, RNF-03); `phases.md`; `risks.md` (RIESGO-01, RIESGO-06); `rules.md`; `architecture.md` (seccion Nivel 3 CUDA); `coding-standards.md`; `directory-structure.md`; `constraints.md` (seccion Entorno Fase 4); `stack.md`; `current-phase.md`; `code/python/sequential.py` y `code/python/common.py` (Fase 1) como referencia de logica Score/AUC; `code/C_OpenMP_MPI/scoring_openmp.c` (Fase 2) como referencia de SplitMix64 y AUC con empates; `code/CUDA/scoring_kernel.cu` y `code/CUDA/scoring_pycuda.py` (placeholders a reemplazar); `code/results/benchmark.csv` (10 columnas con `speedup_vs_python`, filas previas intocables).

La Fase 4 corresponde a la aceleracion GPU con CUDA en notebook para Google Colab (`CUDA/scoring_cuda.ipynb`, entregable principal; `scoring_kernel.cu` y `scoring_pycuda.py` como fuentes derivadas). En esta conversacion NO implementar todavia: primero un plan tecnico claro que cubra los 13 puntos solicitados (1-estructura del notebook y orden de celdas, 2-setup Colab y verificacion GPU con modelo registrado, 3-carga de `.npy` desde `code/data/n_50/` en el runtime, 4-RNG SplitMix64 en device `__device__` sembrado con `seed+k` para Dirichlet en GPU o alternativa W_pool pre-generado en host y transferido, 5-kernel `scoring_kernel` un hilo por candidato con `__shared__` memory para A por bloque BLOCK_SIZE=256 calculando P=W·[T,S,F]/Score=A·P/AUC con empates +0.5·ties, 6-reduccion para best_auc+best_k en GPU o `np.argmax` en host y justificacion, 7-compilacion `%%writefile scoring_kernel.cu`+`nvcc -O2` o `pycuda.compiler.SourceModule` y justificacion, 8-orquestacion PyCUDA H2D/D2H transferencias + lanzamiento grid `ceil(K/256)` bloques, 9-timing que excluye carga de datos con `cudaEvent_t` o `time.perf_counter`+`cuda.Context.synchronize()`, 10-self-test caso pequeno N=3/K=100 validando AUC con empate=0.875 e `|ΔAUC|<1e-4` vs Python secuencial, 11-registro `CUDA` en benchmark.csv 10 columnas DEC-13 `workers=1` `speedup=1.0` `efficiency=1.0` `speedup_vs_python=T_python_sec/T_cuda`, 12-validacion AUC in [0.5,1.0] e `|ΔAUC|<1e-4` vs Python secuencial y consistencia>=0.8, 13-speedup>=5x vs Python secuencial RNF-03 con modelo de GPU registrado y entorno Colab). Restricciones: sin modificar `scoring_openmp.c`, `scoring_mpi.c`, `npy_io.{h,c}`; sin tocar los `.npy` ni filas previas ni esquema del CSV; `scoring_kernel.cu` < 400 LOC; sin nuevas dependencias mas alla de PyCUDA (ya en stack); BLOCK_SIZE=256 (DEC-05); seed=42 (DEC-03); `workers` en CSV = 1 GPU (no hay barrido P, solo un punto de medicion).

### respuesta
Plan tecnico de la Fase 4 (CUDA en Google Colab) — NO se implemento codigo en esta iteracion (solo planificacion), conforme a lo pedido. Se reviso toda la documentacion y los fuentes de referencia (`sequential.py`/`common.py`, `scoring_openmp.c`, placeholders CUDA, `benchmark.csv`).

Decisiones de estrategia recomendadas (propuesta DEC-15):

1. **Estructura del notebook (orden de celdas)**: (0) markdown intro + parametros (N=50, K=100000, seed=42, BLOCK_SIZE=256); (1) verificacion GPU `!nvidia-smi` + registrar modelo; (2) import/instalacion PyCUDA; (3) carga y validacion de `.npy` (espejo de `validate_dataset`); (4) `%%writefile scoring_kernel.cu` (kernel + RNG `__device__`); (5) compilacion via `SourceModule(-O2)` (+ `nvcc -O2` opcional como sanity); (6) orquestacion PyCUDA (H2D una vez, lanzamiento, D2H); (7) reduccion `np.argmax` en host + reconstruccion `best_W`; (8) timing; (9) self-test (empate=0.875 + n_3/K=100); (10) validacion (AUC∈[0.5,1], |ΔAUC|<1e-4, consistencia≥0.8); (11) registro fila `CUDA` en CSV; (12) prints resumen.
2. **Setup/GPU**: `!nvidia-smi`, capturar nombre de GPU (T4 tipico) en variable y en el informe (RIESGO-06); PyCUDA ya en stack (instalar solo si falta).
3. **Carga `.npy`**: subir `code/data/n_50/{matrix_A,profiles,labels}.npy` al runtime (files.upload / clone / Drive); `np.load` + validacion de shapes/dtypes/rangos identica a `common.validate_dataset`. `A` (10×N f32 C-order), `profiles` (N×3 f32: T,S,F), `labels` (10 i32 = [0]*5+[1]*5).
4. **RNG (recomendado: SplitMix64 `__device__` con `seed+k`)**: copiar `splitmix64_next/uniform01/sample_dirichlet` de `scoring_openmp.c` como `__device__`; cada hilo reconstruye `W_k` desde el indice global `k` → identico a Fases 2/3, sin transferir `W_pool` (ahorra K×3 floats), determinista (DEC-12). Alternativa W_pool en host SOLO con el mismo SplitMix64 (NO `np.random.dirichlet`/PCG64). Equivalencia es por VALOR de AUC (DEC-12), no por `best_W`.
5. **Kernel `scoring_kernel`**: 1 hilo = 1 candidato; `__shared__` cachea `A` (10×N≈2 KB) y `profiles` (carga cooperativa + `__syncthreads()`); cada hilo: `W_k`→`P_i=W0·T+W1·S+W2·F` (array local, N≤MAX_ITEMS)→`Score_j=Σ A[j,i]·P_i` (10)→AUC por conteo de pares 5×5 con empates +0.5; escribe `auc_out[k]`. BLOCK_SIZE=256 (DEC-05).
6. **Reduccion (recomendado: `np.argmax` en host)**: D2H de `auc_out` (K f32 = 400 KB, barato) + `np.argmax` (primer maximo → desempate por menor k, igual a Fase 1/3); reconstruir `best_W=sample_dirichlet(seed+k*)`. Justificacion: kernel de reduccion añade complejidad (presupuesto <400 LOC) sin ganancia a esta escala.
7. **Compilacion (recomendado: `%%writefile` + `SourceModule(-O2)`)**: `%%writefile scoring_kernel.cu` materializa el fuente derivado (DEC-09/nomenclatura); se carga su contenido en `pycuda.compiler.SourceModule(..., options=["-O2"])` para orquestar en el mismo flujo JIT; `nvcc -O2` opcional como chequeo de sintaxis. El `.cu` para SourceModule lleva solo `__device__`+`__global__` (sin `main`); <400 LOC.
8. **Orquestacion PyCUDA**: H2D una sola vez de `A`/`profiles`/`labels`; `mem_alloc` de `auc_out` (K f32); lanzamiento `grid=(ceil(K/256),1)`, `block=(256,1,1)`, args `(A,profiles,labels,auc_out,n_items,K,seed)`; D2H de `auc_out`. `CUDA_CHECK` para errores.
9. **Timing (recomendado)**: `cudaEvent_t` para el kernel puro (analisis) + `time.perf_counter` con `cuda.Context.synchronize()` envolviendo [kernel + D2H + argmax] = tiempo de busqueda de W* registrado en CSV. Se EXCLUYE la carga `.npy` y la H2D unica de A/profiles (analoga a "carga de datos", `rules.md`).
10. **Self-test**: (a) test unitario del conteo AUC con empate → 0.875 exacto; (b) extremo-a-extremo `n_items=3`, K=100 comparando `best_auc` CUDA vs una referencia Python que use el MISMO SplitMix64 (mirror) → |ΔAUC|≈0; nota: comparar contra `sequential.py` (PCG64) en caso chico no es apples-to-apples por RNG distinto; la equivalencia formal contra Python secuencial se acredita en n_50 donde ambos llegan a AUC=1.0.
11. **Registro CSV**: 1 fila `CUDA,50,100000,1,best_auc,T_cuda,K/T_cuda,1.0,1.0,T_python_sec/T_cuda`. `T_python_sec`=96.30376639 (fila `Python secuencial` n_50/K=100000 del CSV). Generada en Colab → transcribir/appendear a `code/results/benchmark.csv` sin tocar filas previas ni esquema (10 cols, DEC-13).
12. **Validacion**: `best_auc∈[0.5,1.0]` (=1.0), `|ΔAUC|<1e-4` vs Python secuencial (=0), consistencia ec.4 ≥0.8 (=2.0).
13. **Rendimiento**: `speedup_vs_python=96.30/T_cuda` debe ser ≥5× (RNF-03); con T4 se espera holgadamente. Registrar modelo de GPU y mitigar RIESGO-06 (seed fijo, repetir y promediar, conservar fuentes).

Riesgos/puntos abiertos señalados: RNG mismatch en el self-test chico (resuelto con mirror SplitMix64), exclusion de H2D del cronometro (decision documentada), y el hecho de que el CSV se genera en Colab y debe appendearse al repo. Restricciones respetadas: sin tocar `scoring_openmp.c`/`scoring_mpi.c`/`npy_io.{h,c}`/`.npy`/filas previas/esquema; `.cu`<400 LOC; sin nuevas deps; BLOCK_SIZE=256; seed=42; workers=1. Siguiente paso ofrecido: persistir el plan en `active-tasks.md`, `decisions.md` (DEC-15), `phases.md` y `current-phase.md` (estado PLANIFICADA).

## iteracion 2
### prompt
Tarea 1: No ejecutes nada aqui, plasma toda esta planificación, en lo correspondiente en context/ (En todos los archivos que requieran) tasks, current phase, ...


### respuesta
Planificacion persistida en `context/` (sin ejecutar codigo) y prompt de implementacion dejado planteado en `instruction.md` (la referencia del usuario a "fase 3" se interpreto como esta planificacion de la Fase 4).

Tarea 1 — documentos actualizados:
- `context/project/decisions.md`: añadido **DEC-15** (Estrategia de la Fase 4 CUDA, estado PLANIFICADA): RNG SplitMix64 `__device__` con `seed+k` sin transferir W_pool, equivalencia por valor de AUC, kernel `scoring_kernel` con `__shared__` para A/profiles y AUC con empates, reduccion `np.argmax` en host, compilacion `%%writefile`+`SourceModule(-O2)`, orquestacion PyCUDA H2D/D2H, timing que excluye carga, self-test (empate=0.875 + n_3/K=100 vs mirror), fila `CUDA` (workers=1, speedup=1.0, efficiency=1.0, speedup_vs_python=96.30376639/T_cuda), entorno Colab/T4. Incluye interpretaciones descartadas.
- `context/state/active-tasks.md`: reescrito como "Active Tasks — Fase 4 (CUDA)", estado PLANIFICADA, con hallazgos verificados, las 15 tareas (estructura del notebook, setup/GPU, carga `.npy`, RNG device, kernel, reduccion, compilacion, orquestacion, timing, self-test, registro CSV, validacion, rendimiento, criterios de salida, comandos Colab) y restricciones. Nota explicita: ejecucion en Colab, no en entorno local.
- `context/project/phases.md`: Fase 4 de `[PENDIENTE]` → `[PLANIFICADA — 2026-06-16]` con resumen ordenado de tareas y refs a DEC-15/active-tasks.
- `context/state/current-phase.md`: cabecera de Fase activa actualizada a PLANIFICADA; añadida seccion "Plan Fase 4" y "Proxima accion" (implementar en Colab; prompt planteado en instruction.md; RIESGO-01).


## iteracion 1
### prompt
Ejecuta la implementacion de la Fase 2 (C + OpenMP) siguiendo el plan ya aprobado.

Fuente de verdad del plan (no improvises, sigue estas tareas en orden):

- `context/state/active-tasks.md` (plan detallado de Fase 2, tareas 1-10 y criterios de salida)
- `context/state/current-phase.md` (resumen del plan y proxima accion)
- `context/project/decisions.md` -> DEC-12 (parser .npy, RNG por candidato, AUC con empates, etiqueta "C OpenMP"), y DEC-06/DEC-10
- `context/project/phases.md` (Fase 2), `context/project/risks.md` (RIESGO-02, RIESGO-03)
- `context/state/known-issues.md` -> ISSUE-002, ISSUE-008
- Referencia de comportamiento/CLI/metricas a igualar: `code/python/common.py`, `code/python/sequential.py`, `code/python/multicore.py`
- `traceability_data/2026_06_15_14-24.md` (planificacion registrada)

Alcance de la implementacion:

1. Crear `code/C_OpenMP_MPI/npy_io.h` y `npy_io.c`: parser `.npy` v1.0 minimo (sin librerias externas), solo lectura de `matrix_A.npy` (`<f4`, 10xN), `profiles.npy` (`<f4`, Nx3) y `labels.npy` (`<i4`, 10). No modificar los `.npy` (DEC-10).
2. Reescribir `code/C_OpenMP_MPI/scoring_openmp.c` (< 400 LOC, logica separada de `main`, sin VLAs): carga + validacion (espejo de `validate_dataset`), RNG SplitMix64 por candidato (`seed+k`) -> Dirichlet(1,1,1), `P=W1*T+W2*S+W3*F`, `Score=A*P`, AUC con manejo de empates (`(concordantes+0.5*empates)/(pos*neg)`), consistencia (ec. 4), Random Search con OpenMP (best local por hilo + `#pragma omp critical`), `omp_get_wtime()` solo sobre la busqueda, CLI (`--n-items`, `--k-candidates`, `--seed`, `--threads`, `--self-test`) y registro `C OpenMP` en `results/benchmark.csv` (9 columnas, append-only, leyendo `t_seq` de la fila `Python secuencial` coincidente).
3. Ajustar `code/C_OpenMP_MPI/Makefile`: enlazar `npy_io.c` en el target `openmp`, agregar `-Wextra`; dejar el target `mpi` intacto.

Validacion y criterios de salida (RNF-03/constraints.md):

- `--self-test` de la funcion AUC en verde, incluido un caso con empate vs valor conocido de sklearn (RIESGO-03).
- `|best_auc_C - best_auc_PythonSecuencial| < 1e-4` (mismo K=100000, seed=42, N=50).
- `best_auc` en [0.5, 1.0] (se espera 1.0) y consistencia >= 0.8.
- `speedup >= 3x` con P=4; reportar curva P en {1,2,4,8}.
- Sin fugas (`valgrind --leak-check=full`).
- Compilar con `gcc -O2 -Wall -Wextra -fopenmp` y ejecutar con CWD=`code/` (WSL2, RIESGO-02).

Restricciones: sin MPI ni CUDA en esta fase; sin nuevas dependencias Python ni librerias C externas; sin cambiar las firmas canonicas ni el esquema de `benchmark.csv` de Fase 1; sin modificar `matrix_A.npy`, `profiles.npy` ni `labels.npy`. Al terminar, actualizar `context/state/` (current-phase, progress, known-issues ISSUE-002/008) y `context/project/phases.md` con los resultados reales.

### respuesta
Fase 2 (C + OpenMP) implementada y validada siguiendo el plan de `active-tasks.md`/DEC-12, sin
desviaciones de alcance.

**Archivos**:
- Nuevos `code/C_OpenMP_MPI/npy_io.h` (22 LOC) y `npy_io.c` (126 LOC): parser `.npy` v1.0
  propio (magic + versión + `header_len` LE de 2 bytes + parseo de `'shape': (...)`, valida
  `descr` `<f4`/`<i4` y `fortran_order: False`), sin librerías externas, solo lectura
  (DEC-06/DEC-10). `npy_load_f32`/`npy_load_i32` reutilizables por `scoring_mpi.c` (Fase 3).
- `code/C_OpenMP_MPI/scoring_openmp.c` reescrito (394 LOC, < 400, lógica separada de `main`,
  sin VLAs): RNG SplitMix64 sembrado por candidato (`seed+k`) → Dirichlet(1,1,1)
  (`sample_dirichlet`); `compute_P`/`compute_score` (ecs. 1-2); `compute_auc` con empates a
  0.5 (ec. 3, corrige ISSUE-008); `scoring_consistency` (ec. 4); `load_dataset`/
  `free_dataset` (espejo de `validate_dataset`: shapes, `labels==[0]*5+[1]*5`, T/S∈[0,1],
  F∈{0,1,2}); `random_search_openmp` (best local por hilo + `#pragma omp critical`,
  `omp_get_wtime()` solo sobre la búsqueda, sin sección crítica en el muestreo por DEC-12);
  `self_test` (3 casos vs valores de sklearn); `read_sequential_time`/`append_benchmark`
  espejo de `common.py` (esquema de 9 columnas, append-only); CLI
  `--n-items/--k-candidates/--seed/--threads/--self-test` con `getopt_long`.
- `code/C_OpenMP_MPI/Makefile`: `npy_io.c` enlazado en `scoring_openmp`, `-Wextra` agregado a
  `CFLAGS`, alias `.PHONY openmp`/`mpi` → `scoring_openmp`/`scoring_mpi` (compatibilidad con
  `make openmp`/`make mpi` de `stack.md`/`active-tasks.md`); target `mpi`/`scoring_mpi.c` sin
  cambios.

**Compilación**: `gcc -O2 -Wall -Wextra -fopenmp scoring_openmp.c npy_io.c -o scoring_openmp
-lm` — limpia, sin warnings (gcc 13.3, WSL2, nproc=20).

**Validación (todo en verde, ejecutado con CWD=`code/`)**:
- `--self-test`: 3/3 OK, incluido el caso con empate
  (`scores=[1,2,2,3]`/`y=[0,0,1,1]` → AUC=0.875, igual a `sklearn.roc_auc_score`; además
  separable→1.0 y todo-empate→0.5).
- Equivalencia AUC (N=50, K=100000, seed=42): `best_auc_C = 1.0000` vs
  `best_auc_PySeq = 1.0000` → `|ΔAUC| = 0 < 1e-4` ✓. `best_auc ∈ [0.5,1.0]` ✓. Consistencia
  (ec. 4) = 2.0000 ≥ 0.8 ✓ en los 4 runs.
- Curva de speedup vs Python secuencial (`t_seq=80.97405759998946`): P=1→2386.10×,
  P=2→5555.14×, P=4→10411.83× (**≥3× ✓**, muy por encima del mínimo RNF-03), P=8→14813.67×.
  Escalado interno C-vs-C (P=1 como base): P=2≈2.33×, P=4≈4.36×, P=8≈6.21× (sub-lineal en
  P=8, esperado: cómputo por candidato muy pequeño para N=50). Los valores de
  speedup/efficiency frente a Python son grandes por diseño (S_base=Python secuencial,
  `rules.md`); se documenta para el informe de Fase 6.
- `valgrind --leak-check=full --show-leak-kinds=all` (K=2000, P=4): `definitely lost: 0 bytes`,
  `indirectly lost: 0 bytes`. Los bloques "possibly lost"/"still reachable" restantes son
  internos de `libgomp` (pool de hilos OpenMP/TLS), no del código propio. `valgrind` no estaba
  instalado en el entorno; se instaló vía `apt-get` (paquete del sistema, sin tocar el
  proyecto).
- `results/benchmark.csv`: 4 filas nuevas `C OpenMP` (n_items=50, k_candidates=100000,
  workers∈{1,2,4,8}) añadidas de forma append-only; las 4 filas previas de Fase 1 quedan
  intactas. Se descartaron 2 filas exploratorias de humo (K=1000 y K=2000, generadas durante
  pruebas de compilación/valgrind) para no contaminar la fuente de verdad de métricas.

**Documentación actualizada con resultados reales**: `context/project/phases.md` (Fase 2 →
`[COMPLETADA — 2026-06-15]`, checklist y resumen de resultados), `context/state/current-phase.md`
(tabla de resultados Fase 2, próxima acción = Fase 3), `context/state/progress.md` (Fase 2 →
Completado, Fase 3 → En progreso/pendiente), `context/state/active-tasks.md` (estado →
COMPLETADA, criterios de salida marcados con resultados reales), `context/state/known-issues.md`
(ISSUE-002 e ISSUE-008 → resueltos).

Sin desviaciones de las restricciones: no se usó MPI/CUDA, no se agregaron dependencias Python
ni librerías C externas, no se modificaron `matrix_A.npy`/`profiles.npy`/`labels.npy`, y no se
cambiaron las firmas canónicas ni el esquema de `benchmark.csv` de Fase 1.

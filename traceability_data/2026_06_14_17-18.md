## iteracion 1
### prompt
Vamos a implementar la Fase 1 (Python Baseline) del proyecto Scoring Metagenómico HPC.

El plan técnico ya fue discutido y confirmado con el usuario en una conversación previa (ver `traceability_data/2026_06_14_16-42.md`) y está documentado en:

- `context/state/active-tasks.md` — plan de ejecución paso a paso (fuente de verdad para esta tarea).
- `context/state/current-phase.md` — resumen del plan confirmado.
- `context/project/decisions.md` — DEC-11 (señal diferencial en `generate_data.py` y regeneración de `data/n_50/`/`data/n_100/`).
- `context/project/phases.md` — checklist de la Fase 1.
- `context/.IA/rules.md` — esquema nuevo de `results/benchmark.csv` (9 columnas) y firmas canónicas de `random_search` / `random_search_multicore`.
- `context/state/known-issues.md` — ISSUE-004 a ISSUE-007 (placeholders actuales rotos, falta de señal diferencial, esquema antiguo de benchmark, `pyproject.toml` inexistente).

Ejecuta las 7 tareas de `context/state/active-tasks.md` **en el orden indicado**, validando cada una antes de pasar a la siguiente:

1. **DEC-11**: modificar `code/data/generate_data.py` para inyectar señal diferencial en las filas enfermas de `A` (correlación con ítems de `T`/`F` altos), manteniendo shapes/dtypes de DEC-10. Regenerar `data/n_50/` y `data/n_100/` con `seed=42`. Verificar que exista un `W` del simplex con AUC > 0.6 antes de continuar.
2. Crear `code/python/common.py` con las funciones compartidas exactamente como se especifican en `active-tasks.md` (`load_dataset`, `validate_dataset`, `sample_candidates`, `score_samples`, `evaluate_candidates`, `scoring_consistency`, `compute_metrics`, `append_benchmark`, `read_sequential_time`).
3. Reescribir `code/python/sequential.py` con `random_search(A, profiles, y, K=100_000, seed=42)`, CLI (`--n-items`, `--k-candidates`, `--seed`), timing con `perf_counter` excluyendo la carga, y registro en `results/benchmark.csv`.
4. Reescribir `code/python/multicore.py` con `random_search_multicore(A, profiles, y, K=100_000, seed=42, workers=cpu_count())`, candidatos generados una sola vez en el proceso principal, repartidos con `np.array_split` + `multiprocessing.Pool`, CLI (`--n-items`, `--k-candidates`, `--workers`, `--seed`), cálculo de `speedup`/`efficiency` contra la fila secuencial de `benchmark.csv`.
5. Migrar `code/results/benchmark.csv` al esquema de 9 columnas (`implementation,n_items,k_candidates,workers,best_auc,time_seconds,candidates_per_second,speedup,efficiency`), append-only.
6. Crear pruebas en `code/python/tests/test_baseline.py` (equivalencia AUC secuencial↔multicore <1e-4, AUC∈[0.5,1], consistencia≥0.8, caso chico N=3/K=100).
7. Ejecutar los comandos de verificación listados en `active-tasks.md` (desde `code/`) y confirmar los criterios de salida de la Fase 1.

Restricciones importantes:

- No modificar la estructura de archivos definida en DEC-10 (`matrix_A.npy`, `profiles.npy`, `labels.npy`, `data/n_{n_items}/`); solo cambian los valores generados por DEC-11.
- No usar `T.npy`/`S.npy`/`F.npy` separados ni cambiar el modelo del simplex (sigue siendo 3D: W₁+W₂+W₃=1).
- No implementar C, MPI, OpenMP ni CUDA en esta fase.
- Antes de regenerar los `.npy` en `data/` (tarea 1) y antes de crear `code/pyproject.toml` si no existe (ISSUE-007, necesario para declarar `numpy`/`scikit-learn`/`pytest`), confirma con el usuario el enfoque concreto si hay ambigüedad — pero no es necesario detener todo el plan, solo esos puntos puntuales si surge una duda real.
- Sigue el flujo de commits atómicos sugerido en `context/.IA/instructions.md` (`feat(python): ...`, etc.) si el usuario pide hacer commits.

Al finalizar, resume qué se implementó, los resultados obtenidos (`best_auc`, `best_W`, `time_seconds`, `speedup`, `efficiency` para ambas versiones) y si se cumplieron los criterios de salida de la Fase 1 definidos en `context/state/active-tasks.md`. Actualiza `context/state/current-phase.md`, `context/state/progress.md`, `context/state/active-tasks.md` y `context/state/known-issues.md` para reflejar el nuevo estado (Fase 1 completada o detalles pendientes).

### respuesta
Se implementó completamente la Fase 1 (Python Baseline) siguiendo las 7 tareas de
`context/state/active-tasks.md`, en orden, validando cada una:

1. **DEC-11**: `code/data/generate_data.py` ahora inyecta señal diferencial en las filas
   enfermas (5-9) de `A` — Dirichlet sesgada por `importance = T + F` con
   `SIGNAL_STRENGTH = 8.0` (las sanas usan Dirichlet uniforme). Se regeneraron
   `data/n_50/` y `data/n_100/` con `seed=42`, manteniendo shapes/dtypes de DEC-10
   (`matrix_A.npy` (10,N) f32, `profiles.npy` (N,3) f32, `labels.npy` (10,) i32).
   Verificado antes de continuar: existe W con AUC > 0.6 (de hecho AUC=1.0000) para
   N=50 y N=100.
2. Se creó `code/python/common.py` (140 LOC) con las 9 funciones especificadas:
   `load_dataset`, `validate_dataset`, `sample_candidates`, `score_samples`,
   `evaluate_candidates`, `scoring_consistency` (ec. 4), `compute_metrics`,
   `append_benchmark`, `read_sequential_time`. Importa `generate_data.py` vía
   `importlib` + `sys.path` (sin hacks de import al final del archivo, limpio para ruff).
3. `code/python/sequential.py` reescrito: `random_search(A, profiles, y, K=100_000,
   seed=42)`, CLI `--n-items/--k-candidates/--seed`, carga+validación fuera del
   cronómetro, timing con `perf_counter` solo alrededor de la búsqueda, registro en
   benchmark con `implementation="Python secuencial"`, `workers=1`,
   `speedup=efficiency=1.00`.
4. `code/python/multicore.py` reescrito: `random_search_multicore(A, profiles, y,
   K=100_000, seed=42, workers=cpu_count())`, candidatos generados una sola vez en el
   proceso principal y repartidos con `np.array_split` + `multiprocessing.Pool` (función
   top-level `_evaluate_chunk` picklable para Windows/spawn), CLI con `--workers`,
   `speedup`/`efficiency` calculados contra la fila secuencial vía `read_sequential_time`.
5. `code/results/benchmark.csv` migrado al esquema de 9 columnas
   (`implementation,n_items,k_candidates,workers,best_auc,time_seconds,
   candidates_per_second,speedup,efficiency`), append-only (las filas anteriores eran
   plantilla vacía, no se perdió información real).
6. Se creó `code/python/tests/test_baseline.py` (61 LOC, 5 tests): equivalencia exacta
   secuencial↔multicore en caso chico N=3/K=100 (RIESGO-03), equivalencia <1e-4 en
   N=50, `best_auc ∈ [0.5,1]`, `scoring_consistency >= 0.8`, `best_W` en el simplex
   (suma≈1, ≥0). Se agregó `[tool.pytest.ini_options] pythonpath = ["python"]` a
   `code/pyproject.toml` para que los tests importen `common`/`sequential`/`multicore`
   sin `sys.path` hacks.
7. Verificación final (desde `code/`): `python data/generate_data.py`,
   `python python/sequential.py --n-items 50 --k-candidates 100000 --seed 42`,
   `python python/multicore.py --n-items 50 --k-candidates 100000 --workers 4 --seed 42`,
   `pytest python/tests/ -q` (5 passed), `ruff check python/` y `ruff check data/` (sin
   errores).

**Resultados (seed=42, n_items=50, K=100000)**:

| Implementación        | best_auc | best_W                              | time_seconds | candidates_per_second | speedup | efficiency |
|------------------------|----------|--------------------------------------|--------------|------------------------|---------|------------|
| Python secuencial       | 1.0000   | [0.33742523, 0.32787895, 0.33469582] | 84.3913      | 1184.96                | 1.00    | 1.00       |
| Python multicore (w=4)  | 1.0000   | [0.33742523, 0.32787895, 0.33469582] | 26.6237      | 3756.06                | 3.1698  | 0.7924     |

Consistencia (ec. 4) = 2.0000 en ambas.

**Criterios de salida de Fase 1 — TODOS CUMPLIDOS**:
- `best_auc ∈ [0.5, 1.0]` y consistencia ≥ 0.8 en ambas implementaciones ✅ (1.0000 / 2.0000).
- `|auc_multicore - auc_secuencial| < 1e-4` ✅ (0, exacto, mismo `best_W`).
- `speedup_multicore ≥ 1.5×` (RNF-03) ✅ (3.1698×, workers=4).
- `results/benchmark.csv` con ambas filas en el esquema nuevo, sin pérdida de filas
  previas relevantes (las anteriores eran plantilla vacía) ✅.

**ISSUE-007 (pyproject.toml)**: ya existía `code/pyproject.toml`/`code/uv.lock` con
`numpy`/`scikit-learn`/`matplotlib` + `pytest`/`ruff` (dev). No fue necesario crearlo;
solo se agregó la sección `[tool.pytest.ini_options]`.

Se actualizaron `context/project/phases.md`, `context/state/current-phase.md`,
`context/state/progress.md`, `context/state/active-tasks.md` y
`context/state/known-issues.md` (ISSUE-004 a ISSUE-007 resueltos) para reflejar Fase 1
completada y dejar a Fase 2 (C + OpenMP) como próxima fase (sin iniciar, plan detallado
pendiente de definir).

Durante la sesión también se detectó y limpió un efecto secundario: al ejecutar un
comando de verificación manual desde `code/python/` (en lugar de `code/`), se generó por
error un directorio `code/python/data/n_50/` y `n_100/` con copias de los `.npy`; se
eliminó (`code/data/n_*/` ya está en `.gitignore`, así que no afecta el control de
versiones, pero se limpió para no dejar artefactos confusos en el árbol de trabajo).

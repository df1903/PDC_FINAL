## iteración 1
### prompt
@others/proyecto_final.md  en base a la estructura de carpetas que se propone en este documento, actualiza la estructura de carpeta que existe en @code/ 

### respuesta
Se reestructuró la carpeta `code/` para alinearla con la estructura propuesta en `proyecto_final.md`. Se eliminaron las carpetas obsoletas (`config/`, `review/`, `scripts/`, `src/`, `test/`) y se crearon las carpetas y archivos stub correspondientes a: `data/` (con `generate_data.py`), `python/` (con `sequential.py` y `multicore.py`), `C_OpenMP_MPI/` (con `scoring_openmp.c`, `scoring_mpi.c` y `Makefile`), `CUDA/` (con `scoring_kernel.cu`, `scoring_pycuda.py` y `Makefile`), `results/` (con `benchmark.csv` y subcarpeta `plots/`), y `report/`. El contenido de `generate_data.py` fue tomado directamente del documento del proyecto.

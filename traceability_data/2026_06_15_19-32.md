## iteracion 1
### prompt
Implementa la Fase 3 (C + MPI) siguiendo el plan técnico ya definido. Fuentes de verdad del plan:
`context/state/active-tasks.md` (Fase 3, §1–§13), `context/project/decisions.md#DEC-14`,
`context/project/phases.md` (Fase 3) y `traceability_data/2026_06_15_19-22.md`. No cambies el
plan: ejecútalo. Reutiliza el comportamiento ya validado de la Fase 2.

Implementa, en orden:

1. **`code/C_OpenMP_MPI/Makefile`**: ajusta el target `scoring_mpi` para que linkee `npy_io.c`
   (`mpicc -O2 -Wall -Wextra -o scoring_mpi scoring_mpi.c npy_io.c -lm`). No toques el target
   `scoring_openmp` ni el alias `make mpi`.

2. **`code/C_OpenMP_MPI/scoring_mpi.c`** (reemplazo completo del placeholder, < 400 LOC, lógica
   separada de `main`, sin VLAs, sin OpenMP ni CUDA):
   - Copia **literal** desde `scoring_openmp.c` (sin modificar ese archivo): `splitmix64_*`,
     `sample_dirichlet`, `compute_P`, `compute_score`, `compute_auc` (empates a 0.5),
     `scoring_consistency`, `self_test`, `load_dataset`, `read_last_time`, `append_benchmark`.
   - Reemplaza `<omp.h>` por `<mpi.h>`; elimina lo de OpenMP.
   - Carga **solo en rank 0** (`load_dataset`, fuera del cronómetro) + `MPI_Bcast` de `n_items`,
     `A`, `profiles`, `labels` (los ranks ≠ 0 hacen `malloc` tras recibir `n_items`) (DEC-14).
   - Candidatos por **regeneración local** `sample_dirichlet(seed + k)` con `k` global, **sin
     `MPI_Scatter`** (DEC-12/14).
   - Reparto contiguo por bloques `[r*local_K, (r+1)*local_K)`, último rank absorbe el remanente.
   - Reducción `MPI_Reduce` con `MPI_MAXLOC` y `MPI_DOUBLE_INT` (transporta `best_auc` + `k*`
     global); el root reconstruye `best_W = sample_dirichlet(seed + k*)` y recalcula
     `score`/consistencia para imprimir.
   - Timing `MPI_Wtime()` con `MPI_Barrier(MPI_COMM_WORLD)` justo antes de `t0` y cierre tras el
     `MPI_Reduce`; excluye carga; solo rank 0 mide.
   - CLI `getopt_long`: `--n-items` (50), `--k-candidates` (100000), `--seed` (42),
     `--self-test` — **sin `--threads`** (P = `mpirun -n P`). `--self-test`: solo rank 0 ejecuta
     `self_test` y `MPI_Finalize`.
   - Registro en `results/benchmark.csv` (10 columnas, append-only, **solo rank 0**): etiqueta
     `C MPI`; `workers = size`; `speedup = T_mpi(1)/T_mpi(P)` (baseline propio P=1 vía
     `read_last_time(.., "C MPI", .., required_workers=1, ..)`); `efficiency = speedup/workers`;
     `speedup_vs_python = T_python_secuencial/T_mpi(P)`.

3. **Compilar y ejecutar** (RIESGO-02, WSL2 + OpenMPI; verifica `mpicc --version` primero):
   - `make mpi` desde `code/C_OpenMP_MPI/`.
   - Desde `code/`: `mpirun -n 1 ./C_OpenMP_MPI/scoring_mpi --self-test`, luego el barrido
     `for P in 1 2 4 8; do mpirun -n $P ./C_OpenMP_MPI/scoring_mpi --n-items 50 --k-candidates 100000 --seed 42; done`
     (corre `-n 1` primero para crear la fila baseline `workers=1`). Usa `--oversubscribe` si
     OpenMPI lo exige.

4. **Validar criterios de salida** (`active-tasks.md` §11): `--self-test` 3/3 (incluido empate);
   `|best_auc_MPI − best_auc_PySeq| < 1e-4` (se espera 1.0000 vs 1.0000); `best_auc ∈ [0.5,1.0]` y
   consistencia ≥ 0.8; `speedup(P=4) ≥ 3×` (RNF-03). Si RIESGO-05 lo impide con K=100k, mide
   además K∈{500k,1M} y reporta la curva speedup vs K. (Opcional) `valgrind` con `mpirun -n 4`.

5. **Actualizar documentación** tras implementar y medir: marca Fase 3 `COMPLETADA` en
   `phases.md`, `current-phase.md` (con resultados reales: best_auc, tiempos, speedup/efficiency
   por P, curva K si aplica) y `active-tasks.md` (marcar §11 con `[x]`). Registra la iteración en
   el archivo de trazabilidad de esa conversación.

### respuesta
Fase 3 (C + MPI) implementada y validada completamente. Resumen de lo ejecutado:

**Entorno**: Instalado OpenMPI 4.1.6 via apt-get (RIESGO-02 resuelto). `mpicc` wrappea gcc 13.3.

**1. Makefile** (`code/C_OpenMP_MPI/Makefile`): target `scoring_mpi` actualizado para linkear
`npy_io.c` → `$(MPICC) $(CFLAGS) -o scoring_mpi scoring_mpi.c npy_io.c -lm`. Target `openmp`
y alias `make mpi` sin cambios.

**2. `scoring_mpi.c`** (389 LOC, < 400 ✓): reemplazo completo del placeholder de 28 LOC.
- Copias literales desde `scoring_openmp.c`: `splitmix64_*`, `sample_dirichlet`, `compute_P`,
  `compute_score`, `compute_auc` (empates a 0.5), `scoring_consistency`, `self_test`,
  `load_dataset`, `read_last_time`, `append_benchmark`. `scoring_openmp.c` no modificado.
- `<omp.h>` → `<mpi.h>`; sin OpenMP, sin CUDA, sin VLAs.
- Carga solo en rank 0 + `MPI_Bcast` de `n_items`/`A`/`profiles`/`labels` (DEC-14).
- Regeneración local de candidatos `sample_dirichlet(seed+k)` sin `MPI_Scatter` (DEC-12/14).
- Reparto contiguo `[r*local_K, (r+1)*local_K)`; último rank absorbe remanente.
- `MPI_Reduce` con `MPI_MAXLOC`/`MPI_DOUBLE_INT`: transporta `best_auc` + `k*` global;
  root reconstruye `best_W = sample_dirichlet(seed+k*)`.
- Timing `MPI_Barrier` + `MPI_Wtime()` antes de búsqueda; cierre tras `MPI_Reduce`.
- CLI: `--n-items/--k-candidates/--seed/--self-test` (sin `--threads`).
- Registro `C MPI` en `results/benchmark.csv` (10 cols, append-only, solo rank 0, DEC-13).
- Compilación: `mpicc -O2 -Wall -Wextra` sin warnings.

**3. Ejecución** (desde `code/`, `--allow-run-as-root --oversubscribe`):
- `--self-test`: 3/3 OK (con empate AUC=0.875, separable AUC=1.0, todo empate AUC=0.5).
- Barrido P∈{1,2,4,8} completado.

**4. Resultados** (seed=42, n_items=50, K=100000):

| P | best_auc | time_s  | speedup | efficiency | speedup_vs_python |
|:-:|----------|---------|---------|------------|-------------------|
| 1 | 1.0000   | 0.03993 | 1.0000  | 1.0000     | 2411.99×          |
| 2 | 1.0000   | 0.02242 | 1.7806  | 0.8903     | 4294.80×          |
| 4 | 1.0000   | 0.01094 | 3.6495  | 0.9124     | 8802.50×          |
| 8 | 1.0000   | 0.00742 | 5.3846  | 0.6731     | 12987.52×         |

`best_W = [0.08767833, 0.53756110, 0.37476056]` idéntico para todo P (MAXLOC determinista).

**5. Criterios de salida** (active-tasks.md §11): todos cumplidos:
- `--self-test` 3/3 ✓ | `|ΔAUC|=0 < 1e-4` ✓ | `best_auc=1.0 ∈ [0.5,1.0]` ✓
- `consistencia=2.0 ≥ 0.8` ✓ | `speedup(P=4)=3.65× ≥ 3×` ✓ (RNF-03)
- 4 filas `C MPI` en `benchmark.csv`, filas previas intactas ✓
- RIESGO-05 no materializado (K=100k suficiente para amortizar overhead MPI).

**6. Documentación actualizada**:
- `context/project/phases.md`: Fase 3 marcada `COMPLETADA`.
- `context/state/current-phase.md`: resultados reales Fase 3 + próxima acción Fase 4.
- `context/state/active-tasks.md`: §11 criterios marcados `[x]`.
- `traceability_data/2026_06_15_19-32.md`: este archivo.

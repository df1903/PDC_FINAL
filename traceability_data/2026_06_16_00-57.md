## iteracion 1
### prompt
Implementa la Fase 4 (aceleración GPU con CUDA en notebook para Google Colab) siguiendo el plan ya aprobado: `context/state/active-tasks.md` (15 tareas + criterios de salida), `context/project/decisions.md#DEC-15` y `traceability_data/2026_06_16_00-43.md`. Usa como fuente de verdad el estado real del repositorio y respeta DEC-03 (seed=42), DEC-05 (BLOCK_SIZE=256), DEC-09 (notebook Colab), DEC-10 (datos en `data/n_50/`) y DEC-13 (CSV de 10 columnas).

IMPORTANTE — No ejecutar en este entorno: el repositorio no tiene GPU local (RIESGO-01); el notebook está pensado para ejecutarse después en un runtime GPU de Google Colab. En esta conversación SOLO se crean/editan los archivos (no se corre el notebook, no se compila CUDA, no se mide tiempo real ni se appendea la fila al CSV con tiempos reales). Donde un valor dependa de la corrida en Colab (T_cuda, best_auc real, modelo de GPU), dejar marcadores/placeholders claros y documentar cómo completarlos tras correr en Colab.

Entregables a crear/reescribir (fuentes derivadas, DEC-09):

1. `code/CUDA/scoring_cuda.ipynb` — entregable principal, con las celdas en el orden de la tarea 1 de `active-tasks.md`.
2. `code/CUDA/scoring_kernel.cu` — reemplazar el placeholder: RNG SplitMix64 `__device__`, `__global__ scoring_kernel` con `__shared__` para A y profiles, P/Score/AUC con empates +0.5, solo `__device__`+`__global__`, `< 400 LOC`.
3. `code/CUDA/scoring_pycuda.py` — reemplazar el placeholder: orquestación reusable equivalente a las celdas 4–7, rutas `data/n_{n_items}/` (DEC-10).

Restricciones: sin modificar `scoring_openmp.c`, `scoring_mpi.c`, `npy_io.{h,c}`; sin tocar los `.npy` ni las filas previas ni el esquema de 10 columnas; `scoring_kernel.cu < 400 LOC`; sin dependencias nuevas más allá de PyCUDA; `BLOCK_SIZE=256`; `seed=42`; `workers=1`. Equivalencia por VALOR de AUC (`|ΔAUC|<1e-4`), no por `best_W`. Criterios de salida: tarea 14 de `active-tasks.md`.

### respuesta
Implementé la Fase 4 (CUDA en notebook para Google Colab) creando/reescribiendo los tres
entregables (fuentes derivadas, DEC-09), **sin ejecutar nada en el entorno local** (RIESGO-01):

1. **`code/CUDA/scoring_kernel.cu`** (125 LOC < 400): reemplaza el placeholder. RNG SplitMix64
   `__device__` copiado bit a bit de `scoring_openmp.c` (sembrado `seed+k`); `__global__
   scoring_kernel` (un hilo/candidato) con `__shared__` para `A` y `profiles` (carga cooperativa +
   `__syncthreads()` ANTES del `return` de los hilos con `k>=K` → sin deadlock), `P=W·[T,S,F]` en
   array local (`#define MAX_ITEMS 256`, sin VLAs), `Score=A·P` y AUC por conteo de pares con
   empates +0.5 (acumulado en `double`). Solo `__device__`+`__global__` (sin `main`, para
   `SourceModule`).
2. **`code/CUDA/scoring_pycuda.py`** (160 LOC < 200): reemplaza el placeholder. `random_search_cuda`
   con rutas `data/n_{n_items}/` (DEC-10), `validate_dataset` espejo de `common.py`,
   `SourceModule(-O2)`, H2D una vez, `grid=ceil(K/256)`/`block=256` (DEC-05), `np.argmax` en host,
   y reconstrucción de `best_W` con el MISMO SplitMix64 (mirror Python). Timing
   `perf_counter`+`cudaEvent` que excluye carga y H2D única.
3. **`code/CUDA/scoring_cuda.ipynb`** (28 celdas, 14 código/14 markdown): entregable principal, en
   el orden de la tarea 1 de `active-tasks.md` (0 params → 1 `nvidia-smi`/GPU → 2 setup PyCUDA →
   helpers host → 3 carga+validación → 4 `%%writefile scoring_kernel.cu` → 5 `SourceModule(-O2)` +
   `nvcc -c` sanity → 6 orquestación → 7 reducción/`best_W` → 8 timing → 9 self-test → 10
   validación → 11 fila CSV → 12 resumen). El cuerpo del `%%writefile` es **idéntico** al
   `scoring_kernel.cu` en disco. Se versiona **sin salidas**.

**Verificaciones locales (sin GPU)**: LOC dentro de límite; todas las celdas Python compilan; JSON
nbformat 4 válido. El mirror SplitMix64 (notebook y `scoring_pycuda.py`) es **bit-idéntico**
(`maxdiff=0`) a una reimplementación fiel del RNG de `scoring_openmp.c`; `W(seed+0) =
[0.08767833, 0.53756110, 0.37476056]` coincide con el `best_W` de la Fase 3 (MPI). Los helpers de
host sobre `data/n_50/` real dan AUC empate→0.875, `AUC(W(seed+0))=1.0`, consistencia=2.0 y
`argmax`→k*=0 ⇒ los `assert` de validación del notebook pasarán en Colab.

**Restricciones respetadas**: no se tocaron `scoring_openmp.c`/`scoring_mpi.c`/`npy_io.{h,c}`, los
`.npy`, ni `benchmark.csv` (verificado con `git diff` vacío). `BLOCK_SIZE=256`, `seed=42`,
`workers=1`, sin dependencias nuevas más allá de PyCUDA.

**Pendiente (Colab, marcadores)**: `GPU_NAME`, `best_auc`, `t_search`, `kernel_ms`,
`speedup_vs_python = 96.30376639 / t_search` y la fila `CUDA` del CSV se obtienen al correr el
notebook en runtime GPU; la fila impresa por la celda 11 se transcribe a
`code/results/benchmark.csv` (append-only) y, tras verificar `speedup_vs_python ≥ 5×` (RNF-03), se
marca la Fase 4 como COMPLETADA.

Actualicé el estado del proyecto: `current-phase.md`, `active-tasks.md`, `phases.md` y `progress.md`
quedan en `Fase 4 — IMPLEMENTADA (código) — PENDIENTE corrida en Colab`.
